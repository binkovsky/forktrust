version: 2

project_name: forktrust

before:
  hooks:
    - go mod tidy

builds:
  - id: forktrust
    main: ./cmd/forktrust
    binary: forktrust
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - id: forktrust
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    files:
      - README.md
      - LICENSE

checksum:
  name_template: 'checksums.txt'

snapshot:
  version_template: "{{ incpatch .Version }}-snapshot"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
      - 'Merge pull request'
      - 'Merge branch'

# Homebrew tap is published at binkovsky/homebrew-forktrust and the formula
# is bumped manually on each release (Formula/forktrust.rb). Goreleaser-
# automated tap publishing requires a HOMEBREW_TAP_GITHUB_TOKEN PAT with
# cross-repo write; defer that until owner adds the secret.

release:
  github:
    owner: binkovsky
    name: forktrust
  draft: false
  prerelease: auto
